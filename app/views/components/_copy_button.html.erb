<%# locals: (text:, display: nil) %>
<div class="flex items-center gap-2" x-data="{ copied: false }">
  <code class="bg-navy-800 rounded px-2 py-1 text-violet font-mono text-xs"><%= display || text %></code>
  <button @click="navigator.clipboard.writeText('<%= j text %>'); copied = true; setTimeout(() => copied = false, 2000)"
          class="text-gray-500 hover:text-white transition text-xs" :class="copied && 'text-mint'">
    <span x-show="!copied">Copy</span>
    <span x-show="copied" x-cloak>Copied</span>
  </button>
</div>
