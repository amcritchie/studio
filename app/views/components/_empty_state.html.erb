<%# locals: (message:, detail: nil) %>
<div class="empty-state">
  <p class="text-gray-400 text-lg"><%= message %></p>
  <% if detail.present? %>
    <p class="text-gray-600 text-sm mt-1"><%= detail %></p>
  <% end %>
</div>
