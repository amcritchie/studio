<%# locals: (message:, detail: nil) %>
<div class="empty-state">
  <p class="text-secondary text-lg"><%= message %></p>
  <% if detail.present? %>
    <p class="text-muted text-sm mt-1"><%= detail %></p>
  <% end %>
</div>
